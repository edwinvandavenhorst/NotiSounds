# Compiled app bundle — built locally by build.sh
build/

# macOS metadata
.DS_Store
**/.DS_Store

# Swift build artefacts
*.o
*.d

# Xcode — user-specific files (project file is tracked; these are not)
*.xcworkspace/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscheme.bak
